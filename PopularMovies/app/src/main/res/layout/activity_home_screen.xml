<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.peterark.popularmovies.popularmovies.HomeScreenActivity">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        >


        <!-- Recycler View -->
        <android.support.v7.widget.RecyclerView
            android:id="@+id/movies_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
        </android.support.v7.widget.RecyclerView>


        <!-- Loading Data Progress Bar -->
        <ProgressBar
            android:id="@+id/loading_movies_progress_bar"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_gravity="center"
            android:visibility="invisible"
            />

        <!-- An Error Occurred Message -->
        <TextView
            android:id="@+id/error_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:layout_gravity="center"
            android:visibility="invisible"
            android:text="@string/error_occurred_press_here_to_try_again"/>

        <!-- No movies found Message -->
        <TextView
            android:id="@+id/no_movies_found_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:layout_gravity="center"
            android:visibility="invisible"
            android:text="@string/no_movies_found"/>

    </FrameLayout>
</layout>